# ==============================================
# SocialEcho Server Environment Configuration
# ==============================================

# ------------------
# Database
# ------------------
# MongoDB connection string
# Get from: MongoDB Atlas (cloud.mongodb.com) or local MongoDB
# Example: mongodb+srv://username:password@cluster.mongodb.net/socialecho
MONGODB_URI=your_mongodb_connection_string

# ------------------
# Server Configuration
# ------------------
# Server port (default: 4000)
PORT=4000

# Frontend URL (change to your deployed URL for production)
# Local: http://localhost:3000
# Production: https://your-app.vercel.app
CLIENT_URL=http://localhost:3000

# ------------------
# Security & Authentication
# ------------------
# JWT Secret for access tokens (generate a strong random string)
# Generate using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
SECRET=your_jwt_secret_key_here

# JWT Secret for refresh tokens (generate a different random string)
REFRESH_SECRET=your_jwt_refresh_secret_here

# Encryption key for sensitive data (32 characters for AES-256)
# Generate using: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))"
CRYPTO_KEY=your_32_character_crypto_key_here

# ------------------
# Email Service (for verification emails)
# ------------------
# Email service provider
# Options: gmail, outlook, yahoo, or use smtp.gmail.com directly
EMAIL_SERVICE=gmail

# Your email address
EMAIL=your-email@gmail.com

# Email password or App Password
# For Gmail: Use App Password (https://myaccount.google.com/apppasswords)
# Steps: Google Account → Security → 2-Step Verification → App passwords
PASSWORD=your_gmail_app_password

# ------------------
# Content Moderation APIs (OPTIONAL)
# ------------------
# These are optional - the app will work without them
# Content moderation features will be disabled if not configured

# TextRazor API (for text analysis and classification)
# Get from: https://www.textrazor.com/
# Free tier: 500 requests/day
TEXTRAZOR_API_KEY=your_textrazor_api_key
TEXTRAZOR_API_URL=https://api.textrazor.com/

# Google Perspective API (for toxic content detection)
# Get from: https://perspectiveapi.com/
# Free to use with Google Cloud account
PERSPECTIVE_API_KEY=your_perspective_api_key
PERSPECTIVE_API_DISCOVERY_URL=https://commentanalyzer.googleapis.com/$discovery/rest?version=v1alpha1

# Classifier API (custom ML classifier - optional)
CLASSIFIER_API_URL=http://localhost:5000

# Interface API (additional content analysis - optional)
INTERFACE_API_KEY=your_interface_api_key
INTERFACE_API_URL=your_interface_api_url
